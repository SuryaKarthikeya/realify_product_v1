# Test-only dependencies (not installed in the production image). Install with:
#   python3 -m pip install -r requirements-dev.txt
-r requirements.txt
hypothesis>=6.100      # property tests (PDP invariants, agency-plan §1c-1)
