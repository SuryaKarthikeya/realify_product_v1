/**
 * Public API of the Notifications feature.
 *
 * The notification drawer lives in the app shell but renders this feature's
 * data. Everything else under features/notifications/ is internal.
 */
export { notificationsData } from '@/features/notifications/data/notificationsData';
