/**
 * Public API of the Settings feature.
 *
 * The sidebar renders the settings sub-navigation, so the nav definition is
 * exported here. Everything else under features/settings/ is internal.
 */
export { SETTINGS_NAV_ITEMS } from '@/features/settings/data/settingsNavItems';
